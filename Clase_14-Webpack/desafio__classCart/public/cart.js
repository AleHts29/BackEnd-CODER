const carrito = [];

// Funcion agregar al carrito
function agregarAlCarrito(prodId) {
  // let producto = data.find((item) => item.id == prodId);
  // carrito.push(producto);
  // badge.innerHTML = carrito.length;
  // console.log(carrito);
  // mostrarCompra();
  console.log(prodId);
}
