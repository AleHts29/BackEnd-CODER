// Crear una funcion que haga lo siguiente:
// Recibe 2 parametros “x” , ”y”
// "x" e "y" son números enteros (int)
// Devuelve el número más grande
// Si son iguales, devuelve cualquiera de los dos



const numMayor = (a, b) =>{
    if (a > b){
        console.log(a);
    }
    console.log(b);
}

numMayor(2,5)